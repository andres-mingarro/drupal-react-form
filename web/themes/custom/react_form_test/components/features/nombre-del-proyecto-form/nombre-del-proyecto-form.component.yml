$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: Nombre del Proyecto Form
description: Formulario dinámico "Nombre del proyecto" (categoría, descripción, autor, URL).

libraryOverrides:
  dependencies:
    - drupal_react_form/react-form-app

props:
  type: object
  additionalProperties: false
  properties:
    title:
      type: string
      title: Encabezado opcional sobre el formulario
